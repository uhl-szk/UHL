<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /
    # すでに実在するファイルやフォルダへのアクセスはそのまま通す
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    # それ以外のURL（/Supportなど）はすべてindex.htmlに転送する
    RewriteRule . /index.html [L]
</IfModule>
